version: "3.8"

services:
  nginx:
    image: nginx
    container_name: "nginx"
    ports:
      - 7080:80
      - 7443:443
    volumes:
      - ./nginx:/etc/nginx/template
      - /etc/letsencrypt:/etc/letsencrypt
    environment:
      NGINX_ENVSUBST_TEMPLATE_SUFFIX: ".conf"
