import sys


def process_hostfile(host: str, ip: str) -> None:
    """Insert host and ip from hosts to docker-compose.yml

    Args:
        host (str): hostname
        ip (str): ip address of host
    """
    pass